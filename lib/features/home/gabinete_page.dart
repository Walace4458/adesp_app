import 'package:flutter/material.dart';

class GabinetePage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Card(
      child: Text(
        'GABINETE'
      ),
    );
  }
}